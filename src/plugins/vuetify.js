import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify);

export default new Vuetify({
  theme: {
    themes: {
      // Customize theme colors
      //dark: THEME_COLORS
    },
    dark: true
  }
});
